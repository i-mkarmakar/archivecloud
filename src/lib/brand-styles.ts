/** Landing-navbar primary button look (gradient + soft shadow). */
export const primaryButtonClass =
  "border-transparent bg-gradient-to-b from-primary to-[color-mix(in_srgb,var(--primary)_85%,black)] text-primary-foreground shadow-[0_6px_14px_-8px_color-mix(in_oklch,var(--primary)_10%,transparent)]";

/** Soft primary fill for chips, active nav rows, icon wells. */
export const primarySoftClass = "bg-primary/10 text-primary";

/** Soft primary hover surface. */
export const primarySoftHoverClass = "hover:bg-primary/10";
